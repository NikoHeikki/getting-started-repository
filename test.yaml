pipelines:
  test-run:
    group: sample
    environment_variables:
      GIT_USERNAME: "{{SECRET:[vault][GIT_USERNAME]}}"
    materials:
      acceptancetesting:
        git: https://github.com/NikoHeikki/getting-started-repository.git
        branch: main
        auto_update: false
    stages:
      - run-command:
          jobs:
            run:
              elastic_profile_id: "openjdk8-maven3-oc-az"
              tasks:
               - exec:
                   command: bash
                   arguments:
                    - "-c"
                    - "echo $GIT_USERNAME"
